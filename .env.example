# HealthPilot Configuration
# Copy this file to .env and fill in your values

# --- LLM Provider (choose one) ---
# Options: "anthropic" or "openai"
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-sonnet-4-20250514
# Alternative cheap option: gpt-4o-mini
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.3

# --- API Keys ---
# If using Anthropic (Claude)
# ANTHROPIC_API_KEY=sk-ant-...

# If using OpenAI (GPT) or for embeddings
OPENAI_API_KEY=sk-...

# --- Embeddings (required for RAG) ---
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# --- Vector Store ---
CHROMA_PERSIST_DIRECTORY=./data/chroma_db

# --- Google Calendar (optional) ---
GOOGLE_CREDENTIALS_PATH=./credentials/google_credentials.json
GOOGLE_CALENDAR_ID=primary

# --- Email Integration (optional, for wellness reminders) ---
# SMTP Configuration (Gmail example - use App Password, not regular password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
USER_EMAIL=your-email@gmail.com
USER_NAME=Your Name

# --- LangSmith Observability (optional but recommended) ---
# LangSmith uses LANGSMITH_API_KEY as of 2024+ (replaces deprecated LANGCHAIN_API_KEY)
LANGCHAIN_TRACING_V2=true
LANGSMITH_API_KEY=lsv2_...
LANGCHAIN_PROJECT=healthpilot

# --- USDA API (optional, for live ingestion) ---
USDA_API_KEY=

# --- App ---
LOG_LEVEL=INFO
